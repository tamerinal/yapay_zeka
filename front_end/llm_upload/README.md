# LLM Upload Interface

This Flask application provides a simple web page to upload large
language model files. Uploaded files are saved into the `uploads`
folder next to the application.

## Usage

```bash
pip install -r requirements.txt
python app.py
```

Open your browser at `http://127.0.0.1:5000/` to select and upload a
file.
